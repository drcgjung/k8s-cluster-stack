apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# TODO 17.03.2022 CARSLEN: kubectl apply -k ./ -n argocd !!!
namespace: argocd

resources:
  - namespace.yaml
  - https://raw.githubusercontent.com/argoproj/argo-cd/v2.3.1/manifests/ha/install.yaml

# Configure rbac to grant catenax-ng:ArgoCDAdmins admin rights to argoCD
patchesStrategicMerge:
  - argocd-rbac-cm.yaml
  - github-oauth-argocd-cm.yaml
